"""Pytest-test fixtures (data files, not pytest fixtures).

The replay-regression harness loads `replay_golden.json` from this
directory. The file is auto-created on first run of
tests/test_replay_regression.py with a starter set of behavior
anchors. Edit it directly to memorialize a coaching call as
non-regressable.
"""
